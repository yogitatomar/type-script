// Interfaces
// Interfaces are similar to type aliases, except they only apply to object types.

interface user_type{
    name:string,
    age:number
}

var user:user_type={
   name:'yogita',
   age:20
}

console.log(user);


